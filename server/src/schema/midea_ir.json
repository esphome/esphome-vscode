{
  "midea_ir.climate": {
    "schemas": {
      "CONFIG_SCHEMA": {
        "extends": [
          "climate_ir.CLIMATE_IR_WITH_RECEIVER_SCHEMA"
        ],
        "config_vars": {
          "id": {},
          "use_fahrenheit": {
            "key": "Optional",
            "default": "False",
            "type": "boolean"
          }
        }
      }
    }
  }
}